<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="hills.sms.texter"
    android:versionCode="1"
    android:versionName="1.0" android:installLocation="preferExternal">

    <uses-sdk
        android:minSdkVersion="19"
        android:targetSdkVersion="21" />

    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECEIVE_MMS" />
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.RECEIVE_WAP_PUSH" />
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.WRITE_SMS" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.provider.Telephony.SMS_RECEIVED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />


    <uses-feature android:name="android.hardware.telephony" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>



        <!-- BroadcastReceiver that listens for incoming SMS messages -->
        <!-- Note the use of android:enabled that is linked to a bool. This will mean this receiver
             is enabled on KitKat devices and above -->
        <receiver android:name=".sender.SmsReceiver"
                  android:enabled="@bool/hasKitKat"
                  android:permission="android.permission.BROADCAST_SMS">

            <!-- KitKat+ SMS received action -->
            <intent-filter>
                <action android:name="android.provider.Telephony.SMS_DELIVER" />
            </intent-filter>

        </receiver>

        <!-- BroadcastReceiver that listens for incoming SMS messages -->
        <!-- Note the use of android:enabled that is linked to a bool. This will mean this receiver
             is enabled on preKitKat devices -->
        <receiver android:name=".sender.SmsReceiverLegacy"
                  android:enabled="@bool/preKitKat">

            <!-- Pre-KitKat SMS received action -->
            <intent-filter>
                <action android:name="android.provider.Telephony.SMS_RECEIVED" />
            </intent-filter>

        </receiver>

        <!-- BroadcastReceiver that listens for incoming MMS messages -->
        <!-- Note the use of android:enabled that is linked to a bool. This will mean this receiver
             is enabled on KitKat devices and above -->
        <receiver android:name=".sender.MmsReceiver"
                  android:enabled="@bool/hasKitKat"
                  android:permission="android.permission.BROADCAST_WAP_PUSH">

            <!-- KitKat+ MMS received action -->
            <intent-filter>
                <action android:name="android.provider.Telephony.WAP_PUSH_DELIVER" />
                <data android:mimeType="application/vnd.wap.mms-message" />
            </intent-filter>

        </receiver>

        <!-- BroadcastReceiver that listens for incoming MMS messages -->
        <!-- Note the use of android:enabled that is linked to a bool. This will mean this receiver
             is enabled on preKitKat devices -->
        <receiver android:name=".sender.MmsReceiverLegacy"
                  android:enabled="@bool/preKitKat">

            <!-- Pre-KitKat MMS received action -->
            <intent-filter>
                <action android:name="android.provider.Telephony.WAP_PUSH_RECEIVED" />
                <data android:mimeType="application/vnd.wap.mms-message" />
            </intent-filter>

        </receiver>

        <service android:name="com.android.mms.transaction.TransactionService"/>

        <!-- Service that delivers SMS messages received from the phone "quick response" -->
        <service android:name=".sender.RespondService"
                 android:permission="android.permission.SEND_RESPOND_VIA_MESSAGE"
                 android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.RESPOND_VIA_MESSAGE" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="sms" />
                <data android:scheme="smsto" />
                <data android:scheme="mms" />
                <data android:scheme="mmsto" />
            </intent-filter>
        </service>

        <!-- A service used internally to process incoming SMS/MMS -->
        <service android:name=".sender.MessagingService"
                 android:exported="false" />

        <service android:name="com.hillsalex.metatext.services.SendReceiveServices"
                android:enabled="true"
            />

        <activity
            android:name=".ConversationListActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ConversationDetailActivity"
	        android:windowSoftInputMode="stateHidden" 
            android:label="@string/title_conversation_detail"
            android:parentActivityName=".ConversationListActivity" >
            <intent-filter>
                <action android:name="android.intent.action.SENDTO" />
				<data android:scheme="sms" />
                <data android:scheme="smsto" />
                <data android:scheme="mms" />
                <data android:scheme="mmsto" />
			</intent-filter>
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="hills.sms.texter.ConversationListActivity" />
        </activity>
        <activity
            android:name="com.hillsalex.metatext.RoutingActivity"
            android:theme="@style/NoAnimation.Theme.BlackScreen"
            android:launchMode="singleTask"
            android:configChanges="touchscreen|keyboard|keyboardHidden|orientation|screenLayout|screenSize"
            >


        </activity>
    </application>

</manifest>
